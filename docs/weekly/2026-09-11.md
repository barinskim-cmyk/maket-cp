# Еженедельная сводка — Maket CP — 2026-09-11

## Сделано за неделю

Тихая неделя, один законченный пункт бэклога.

- Упростил интерфейс Rate Setter (`ux-ratesetter-simplify`), задача помечена DONE.
- Обновил бэклог и закоммитил прошлую сводку (2026-09-08).
- Всего 3 коммита за 7 дней. Бэклог: 78 DONE / 37 TODO / 1 WIP (было 76 / 38 / 1 — прогресс небольшой).

## Состояние: CI, Supabase, ошибки

- **CI:** проверить статус из этой сессии не смог — в песочнице нет `gh` и доступа к GitHub Actions. Отмечу: `package-lock.json` в репозитории **есть** (лежит с 02.07), так что «отсутствие lock-файла», названное причиной красных E2E на прошлой неделе, скорее всего не единственная или не настоящая причина. Нужно посмотреть логи упавшего прогона глазами в интерактивной сессии.
- **Supabase:** проект `maketcp` — по-прежнему **INACTIVE** (на паузе с 04.08, уже ~5,5 недель). Запрос к `client_errors` отвалился по таймауту — база спит.
- **Ошибки клиента:** сбор недоступен, пока Supabase на паузе.
- **Открытый баг (WIP):** в просмотре карточек иногда перескакивает на другой проект. Не починен.

## План на следующую неделю (верх бэклога)

P0-задач в TODO не осталось — все закрыты. Верх P1 по-прежнему упирается в паузу Supabase:

- Supabase: миграция `photo_versions` + бакет postprod (**заблокировано** паузой БД).
- Минимальный режим ретушёра (роль retoucher пока без интерфейса).
- Проверка данных на живой съёмке (Exp_Date, ZIMPORTDATE, хэши RAW).
- Вывод заявленных на лендинге метрик + лестница «нет данных → действие».

Не блокируется Supabase и можно двигать сразу: фикс WIP-бага с перескоком проекта; разбор красного CI по логам.

## ВОПРОСЫ К МАШЕ

Вопросы 1 и 3 повторяются уже третью неделю подряд — по ним нет решения, и они продолжают блокировать работу.

1. **Supabase INACTIVE — главный блокер (>5 недель).** Весь облачный блок P1 стоит: миграции, keep-alive, сбор ошибок. Рекомендация: принять решение по инфраструктуре — либо снять лимиты / апгрейднуть текущий проект, либо запускать переезд на РФ (self-hosted Supabase + Yandex Object Storage по плану от 04.08). Без этого следующая неделя снова будет «тихой».
2. **Красный CI (E2E) — нужна ручная проверка.** `package-lock.json` на месте, значит прошлогодний диагноз («нет lock-файла») не подтверждается. Рекомендация: в ближайшей интерактивной сессии открыть логи последнего упавшего прогона Actions вместе — оттуда сразу видна реальная причина; чинить вслепую из песочницы не хочу.
3. **`strategy-2026.md` — «РЕШЕНИЕ MASHA» по хедлайну лендинга всё ещё пустое.** Рекомендация: зафиксировать Вариант 1 («Уберите 1–2 потерянных дня из каждого цикла») — он уже стоит на лендинге и даёт понятный ROI. Одна строка — и вопрос закрыт навсегда.
