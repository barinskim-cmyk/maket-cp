# Сводка Maket CP — неделя до 2026-09-08

## Сделано за неделю

Тихая неделя: 2 коммита, оба 3 сентября, после — тишина.

- Ограничил ширину колонки пайплайна, чтобы не разъезжалась (`ux-pipeline-width`).
- Отметил задачу `ux-pipeline-width` как DONE в бэклоге.

Новых фич и багфиксов не было.

## Состояние

- **CI:** деплой на GitHub Pages — зелёный. E2E-тесты — красные, и это уже 5+ прогонов подряд (с 18.08). Причина теперь ясна и она простая: `npm ci` падает, потому что в репозитории нет `package-lock.json`. Тесты даже не запускаются — падает установка зависимостей. Чинится быстро.
- **Supabase:** проект `maketcp` — INACTIVE (на паузе с 04.08, уже больше 5 недель). Посчитать ошибки в `client_errors` не смог — БД не отвечает (соединение отваливается по таймауту, проект спит).
- **Ошибки клиента:** сбор недоступен, пока Supabase на паузе.
- **Открытый баг (WIP):** в просмотре карточек иногда перескакивает на другой проект. Пока не починен.
- **Бэклог:** 76 DONE / 38 TODO / 1 WIP.

## План на следующую неделю (верх бэклога)

Почти всё сверху P1 упирается в паузу Supabase:

- Supabase: миграция `photo_versions` + бакет postprod (заблокировано паузой).
- Минимальный режим ретушёра.
- Проверка данных на живой съёмке (Exp_Date, ZIMPORTDATE, хэши RAW).
- Вывод заявленных на лендинге метрик + лестница «нет данных → действие».

Не блокируется Supabase и можно двигать сразу: починка E2E (добавить `package-lock.json`), фикс WIP-бага с перескоком проекта.

## ВОПРОСЫ К МАШЕ

Все три вопроса повторяются с прошлой недели — по ним не было решения, и они продолжают блокировать работу.

1. **Supabase INACTIVE — главный блокер (уже >5 недель).** Облако, keep-alive и сбор ошибок стоят. Рекомендация: определиться с инфраструктурой — либо снять лимиты / апгрейднуть текущий проект, либо ускорить переезд на РФ (self-hosted Supabase + Yandex Object Storage по плану от 04.08). Без этого весь облачный блок P1 не двигается.
2. **E2E красные 5+ прогонов подряд.** Причина уточнена: не тестовые сценарии, а отсутствие `package-lock.json` — `npm ci` не может поставить зависимости. Рекомендация: дать добро закоммитить `package-lock.json` следующей сессией — 10-минутная правка, вернёт зелёный CI.
3. **`strategy-2026.md` — «РЕШЕНИЕ MASHA» по хедлайну лендинга всё ещё пустое.** Рекомендация: зафиксировать Вариант 1 («Уберите 1–2 потерянных дня из каждого цикла») — он уже на лендинге и даёт понятный ROI.
