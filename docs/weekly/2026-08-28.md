# Еженедельная сводка Maket CP — 28.08.2026

## Сделано за неделю
- Неделя фактически пустая: за 7 дней 1 коммит — прошлая сводка (`df692d3`, 21.08). Продуктовых изменений и сессий не было.
- Причина застоя прежняя: облачная часть заблокирована недоступной Supabase, а красные E2E-тесты никто не разбирал (см. вопросы).
- Новых багов и задач в бэклоге не появилось.

## Состояние
- **CI:** деплой на GitHub Pages — зелёный. **E2E-тесты — красные** (третий раз подряд, последний прогон 21.08). Разобрал причину: это НЕ поломка окружения — тесты реально запускаются, Playwright доезжает до конца и выгружает отчёт (25 файлов). Падают сами проверки. Отчёт доступен как артефакт прогона на GitHub. Значит, чинить надо конкретные упавшие сценарии, а не сборку. (Ошибка `npm ci` в логах безобидна — есть фолбэк `|| npm install`, потому что `package-lock.json` в git не коммитится.)
- **Supabase:** в этой сессии база **не проверялась** — Supabase-коннектор (MCP) не подключён, прямого доступа к проекту нет. Стоящий блокер с 04.08 никуда не делся: аккаунт org ограничен, проект INACTIVE, `client_errors` и keep-alive недоступны. (В настройках задачи указан ID проекта `mukiyeuxulasvtlpckjf` — отличается от прежнего; уточним, когда база вернётся.)
- **Ошибки клиента:** посчитать не удалось — базы нет.
- **Баги:** новых не поступало (источник — Supabase — недоступен).

## План на следующую неделю (верх бэклога)
- В работе (WIP): **Данные / Время** — проверка на живой съёмке (Exp_Date, ZIMPORTDATE, хэши RAW). Нужна Маша на съёмке — один кадр и сравнение таймштампов.
- Верх очереди TODO, все P1: Роли/Ретушёр (минимальный режим ретушёра), Метрики/UX (вывод метрик лендинга), Редизайн/UI-спека (экраны по фазам), Уведомления фотографу.
- Бэклог: 75 DONE / 39 TODO / 1 WIP.
- Рекомендую следующей сессией всё же взять починку E2E — теперь понятно, что это точечные тесты, а не инфраструктура.

## ВОПРОСЫ К МАШЕ
1. **Supabase INACTIVE с 04.08 — главный блокер.** Весь облак, keep-alive и сбор ошибок стоят третью неделю. Рекомендация: определиться с инфраструктурой — либо снять лимиты/апгрейд текущего проекта, либо ускорить переезд на РФ (self-hosted Supabase + Yandex Object Storage), как в плане от 04.08. Без решения облачные задачи не двигаются.
2. **Красные E2E три прогона подряд.** Теперь диагностировано: падают конкретные тестовые сценарии (не сборка), отчёт есть. Рекомендация: дать добро взять починку в приоритет следующей сессии, пока не накопилось.
3. **strategy-2026.md — «РЕШЕНИЕ MASHA» по хедлайну лендинга всё ещё пустое.** Рекомендация: зафиксировать Вариант 1 («Уберите 1–2 потерянных дня из каждого цикла») — он уже на лендинге и даёт понятный ROI.
