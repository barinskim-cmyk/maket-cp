# Еженедельная сводка Maket CP — 21.08.2026

## Сделано за неделю
- Логика переименования файлов вынесена в отдельный протестированный infra-хелпер (рефакторинг), задача `feat0702-native-rename` закрыта.
- Фикс безопасности: при выходе из аккаунта чистится localStorage-кэш проектов — закрыт остаток утечки превью (RLS-leak).
- Всего 3 коммита за 7 дней. Неделя была неактивной по объёму.

## Состояние
- **CI:** деплой на GitHub Pages — зелёный. **E2E-тесты падают (красный) два прогона подряд** (18 и 19 августа). Требует разбора.
- **Supabase:** проект `maketcp` в статусе **INACTIVE** — база недоступна (запрос к ней отвалился по таймауту). Это тот самый ограниченный аккаунт с 04.08. Пока проект не восстановлен: не читаются `client_errors`, не работает keep-alive, вся облачная часть заблокирована.
- **Ошибки клиента:** посчитать не удалось — база на паузе.
- **Баги:** новых из Supabase не получено (база недоступна).

## План на следующую неделю (верх бэклога)
- **P1 — Supabase: миграция `photo_versions` + бакет postprod.** Запустить `001_photo_versions.sql` в дашборде. Блокируется недоступностью базы (см. вопрос 1).
- **P2 — Desktop UI: загрузка ЦК-превью + сборка COS и выгрузка в облако.**
- Дальше в очереди TODO: минимальный режим ретушёра, вывод метрик лендинга, редизайн экранов по фазам, уведомления фотографу.
- Бэклог: 75 DONE / 39 TODO.

## ВОПРОСЫ К МАШЕ
1. **Supabase INACTIVE** — весь облак и keep-alive стоят, восстановление заблокировано с 04.08. Рекомендация: принять решение по инфраструктуре — либо апгрейд/снятие лимитов текущего проекта, либо ускорить переезд на РФ (self-hosted Supabase + Yandex). Без этого P1-миграция и облачные задачи не двигаются.
2. **E2E-тесты красные** два прогона подряд. Рекомендация: разрешить взять починку E2E в приоритет следующей сессии, пока не разъелось дальше.
3. **strategy-2026.md: не заполнен «РЕШЕНИЕ MASHA»** по хедлайну лендинга. Рекомендация: зафиксировать Вариант 1 («Уберите 1–2 потерянных дня из каждого цикла») — он уже на лендинге и даёт понятный ROI.
