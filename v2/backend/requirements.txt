pywebview>=4.0
Pillow>=9.0
pdfplumber>=0.9
openpyxl>=3.0
watchdog>=4.0
# pynput removed 2026-05-01: it crashes the Python process on macOS 26.4
# (SIGTRAP from TSMGetInputSourceProperty on a non-main thread). Hotkeys
# now use NSEvent global monitor via pyobjc.
pyobjc-framework-ApplicationServices>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
# Read Capture One's .cop (JPEG XL) previews directly so we keep CC and
# control rotation explicitly, instead of going through `sips` which lost
# both. Маша 2026-05-02.
pillow-jxl-plugin>=1.3
