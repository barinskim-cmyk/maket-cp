# Аудит проекта по ролям — 2026-07-02

Роли аудита: фотограф (desktop), клиент (web), ретушёр (web) + два мета-взгляда: предприниматель и senior-инженер. Плюс проверка Supabase через MCP.

Метод: три параллельных code-review агента по v2/frontend + v2/backend + стратегические документы; Supabase — прямая проверка через MCP. Номера строк ориентировочные (код живёт), суть проблем проверяема.

---

## 0. СРОЧНОЕ: Supabase-проект в статусе INACTIVE

Проверено через MCP 02.07.2026: оба проекта (`maketcp`, eu-central-1 и старый `barinskim-cmyk's Project`) в статусе **INACTIVE** (пауза free-tier). Это значит: **веб-клиент, share-ссылки, авторизация и синхронизация сейчас не работают вообще** — ни для клиентов, ни для ретушёров.

- Действие 1: восстановить проект `maketcp` (Dashboard → Restore) и проверить share-ссылку EKONIKA.
- Действие 2: решить вопрос надолго — либо платный тариф (~$25/мес, снимает автопаузу), либо keep-alive пинг (cron-запрос раз в день). Для продукта с живым клиентом автопауза недопустима.
- Действие 3: старый пустой проект `barinskim-cmyk's Project` — удалить, чтобы не путаться.
- Advisors (security/performance) вернули пусто — вероятно из-за паузы; **перепроверить после restore**.

---

## 1. Роль: ФОТОГРАФ (desktop)

### HIGH
| # | Где | Проблема | Рекомендация |
|---|-----|----------|--------------|
| 1.1 | previews.js (pvClearAll) | Очистка превью без backup/undo — один клик может уничтожить работу. Паттерн инцидента 14.04 не закрыт для этого пути | Snapshot в localStorage/IndexedDB перед очисткой + confirm с числом фото |
| 1.2 | shootings.js (browser-режим) | В браузерном фолбэке проект не переживает перезагрузку страницы, если облако недоступно | Автосохранение проекта в localStorage/IndexedDB как последний рубеж |
| 1.3 | cards.js (drag-drop) | JSON.parse в обработчике drop без try/catch — сбой drag-drop молча | try/catch + errReport |
| 1.4 | backend (кодировки) | Захардкоженный UTF-8 при чтении путей/файлов — потенциальная регрессия кириллицы (родня инцидента 15.04) | errors='replace' + прогнать тест с кириллическими именами |
| 1.5 | sync.js / Rate Setter | Нет progress-индикации на больших прогонах и слабая обработка битых .cos | Прогресс N/M + skip повреждённых с итоговым списком |

### MED
- Удаление карточки без undo (есть soft-delete в БД, но локально — нет).
- Галерея на 5000+ фото: полный rerender местами остался; lazy loading с race condition при быстрой прокрутке.
- cpRenderCard — гигантская функция, reflow при каждой навигации.
- localStorage quota не обработана (большие проекты в браузер-режиме).

---

## 2. Роли: КЛИЕНТ и РЕТУШЁР (web)

### HIGH
| # | Где | Проблема | Рекомендация |
|---|-----|----------|--------------|
| 2.1 | shootings.js (~3754) | **Ретушёр как роль почти не существует**: статус «Команда» без интерфейса — нет списка фото на ретуши, загрузки результата, версий, фидбека | Минимальный режим ретушёра: очередь фото → upload результата → версия в photo_versions |
| 2.2 | cards.js (cpMobileRender) | Мобильный клиент по share-ссылке: табы есть, но выбрать фото в слот, прокомментировать, аннотировать — нельзя (drag-drop только desktop) | Tap-выбор в carousel + мобильный UI комментариев/маркеров |
| 2.3 | compare-view.js | Сравнение версий (ЦК/ретушь) заперто за DEBUG-флагом — недоступно ни клиенту, ни ретушёру | Включить или интегрировать в основной UI |
| 2.4 | supabase.js (~347) | Конкурентное редактирование фотограф+клиент: guard от пустого push есть, но merge без версионирования — можно потерять чужие правки | `updated_at`/`_version` на карточку, last-write-wins по полю, не по всему массиву |

### MED
- Запрос доп. кадров возвращает ВСЁ на этап 1 вместо выбранных фото — добавить checkbox-выбор.
- Auto-pull отключён для share-ссылок без realtime-фолбэка — клиент не видит обновления; периодический pull раз в 30–60 сек.
- Падение мобильного рендера теряет черновик выбора — backup перед рендером.
- post-approval-safeguard: выбор view/edit в sessionStorage, легко случайно выбрать edit.

### Безопасность share
- `window._isShareLink` — только UI-гейт, подделывается из консоли. RLS защищает БД, но проверять роль из proj._role при входе в admin-табы (LOW, сделать).
- `sbJoinByToken` написан, но не используется — либо подключить, либо удалить.
- `sbCopyShareUrl` — deprecated execCommand → navigator.clipboard.

---

## 3. Supabase (схема, RLS, миграции)

| # | Находка | Важность | Действие |
|---|---------|----------|----------|
| 3.1 | Проект INACTIVE (см. раздел 0) | CRITICAL | Restore + анти-пауза |
| 3.2 | `023_storage_auth_diagnostic.sql`: `GRANT ALL ON public.debug_log TO anon` — анонимная запись/чтение отладочной таблицы | HIGH | Миграция-откат: DROP debug_log и debug_capture_and_pass; удалить и 024 (trivial test) |
| 3.3 | В нумерации миграций дыра: 031 → 033 (нет 032), нет 001; `all_pending_migrations.sql` и `proposed-photo_versions.sql` лежат рядом с боевыми | MED | Проверить что реально применено в prod (`supabase migration list` после restore), мусор убрать в archive/ |
| 3.4 | `photo_versions` (030/031) активно используется фронтом — применение в prod не подтверждено (проект в паузе) | HIGH | После restore: `SELECT tablename FROM pg_tables WHERE tablename='photo_versions';` |
| 3.5 | `save_cards_by_token` и запись в photo_versions доступны anon по токену — токен = полный write. Роль ссылки (viewer/editor) на уровне БД не различается | MED | В RPC проверять role из share_links; view-ссылки — только SELECT |
| 3.6 | Advisors пустые — не подтверждение чистоты, а следствие паузы | — | Перепроверить get_advisors(security) после restore |

---

## 4. Взгляд ПРЕДПРИНИМАТЕЛЯ

### HIGH
1. **Монетизация не сведена в одну матрицу.** В документах четыре несогласованных SKU: 500₽/проект, пакет 10×400₽, anchor 30к/мес, brand-annual без цены. Это блокирует первый платёж и любой outbound. → Заполнить `monetization-matrix.md`: кто платит, за что, когда, upgrade-path. 1 час работы.
2. **Один платящий клиент = 100% выручки.** Уход EKONIKA обнуляет план. → Цель: ≥2 активных клиента к концу Q3; 1–2 тёплых лида из фотографов-евангелистов сейчас.
3. **Позиционирование расходится в трёх местах**: landing_texts.md («согласование в 2 раза»), landing.html («1–2 потерянных дня»), внутренние доки (Version A). → Выбрать один headline (рекомендация: «1–2 потерянных дня» — понятный ROI) и синхронизировать.
4. **Нет метрик использования.** Не видно DAU, завершённые проекты, где ломается workflow. Ставка на евангелистов требует цифр «до/после». → Минимум: счётчики в action_log + еженедельная сводка (DAU, активные проекты, топ-ошибки).

### MED
- Desktop-установка Python-приложения — главный friction онбординга фотографа. Не решать сейчас, но в Q3 исследовать: упрощённый инсталлер или браузерный Rate Setter.
- Per-project 500₽ не масштабируется выше ~5 съёмок/мес — подготовить upgrade-path на пакет, не меняя модель сейчас.
- Юр-пакет (оферта, ПД, DPA) — по Q2-плану должен быть закрыт; если нет — это блокер первого платежа.

---

## 5. Взгляд SENIOR-ИНЖЕНЕРА

### Зрелость
- **Тесты**: e2e-сценарии Rate Setter лежат в `tests/e2e/pending/*.draft` — ни один не запускается. CI (`e2e.yml`) с `continue-on-error: true` — красный прогон ничего не блокирует.
- **Staging**: нет; всё тестируется на проде с данными EKONIKA (решение о Supabase branching принято ранее — не реализовано).
- **Объём**: ~64k строк JS; previews.js ≈ 6.3k, articles.js ≈ 5.3k, supabase.js ≈ 5k, shootings.js ≈ 4.6k, cards.js ≈ 4.5k. Глобальное состояние + HTML строками — работает, но регрессии всё вероятнее.
- **Наблюдаемость**: error-reporter + diag есть (хорошо), но нет real-time алертов; Python-часть не шлёт ошибки в облако.

### Топ-5 инвестиций по ROI (для соло-владельца)
| # | Что | Усилие | Эффект |
|---|-----|--------|--------|
| 1 | Убрать `continue-on-error` из CI | 15 мин | Красный тест снова что-то значит |
| 2 | Активировать 3–5 draft-тестов Rate Setter | 2 ч | Покрыт самый рискованный модуль |
| 3 | Restore Supabase + проверка photo_versions в prod | 30 мин | Снимает риск «код пишет в несуществующую таблицу» |
| 4 | Staging (Supabase branch или второй проект) | 3 ч | Тесты не на данных клиента |
| 5 | Еженедельная сводка метрик/ошибок | 4 ч | Masha видит прод без опроса клиентов |

### Не делать
- Не переписывать на React/Vue — недели работы, риск регрессий, ноль ценности клиенту.
- Не рефакторить все 5 больших файлов разом — только точечно, при следующем заходе в модуль.

---

## 6. Сводный план (порядок выполнения)

**Сейчас (день):**
1. Restore Supabase + проверить share-ссылку EKONIKA + photo_versions + advisors (0, 3.1, 3.4, 3.6)
2. Откат debug-миграций 023/024 (3.2)
3. CI: снять continue-on-error (5.1)

**Неделя:**
4. Заполнить monetization-matrix.md (4.1)
5. Backup перед pvClearAll + автосейв в браузер-режиме (1.1, 1.2)
6. Активировать draft-тесты Rate Setter (5.2)
7. Единый headline на лендинге и в доках (4.3)

**Месяц (июль):**
8. Мобильный выбор фото + комментарии для клиента (2.2)
9. Минимальный режим ретушёра (2.1)
10. Версионирование карточек для конкурентного редактирования (2.4)
11. Staging + метрики-сводка (5.4, 5.5)
12. Periodic pull для share-ссылок (2. MED)

Задачи 1–12 добавлены в backlog.html (префикс `audit0702-`).
